apiVersion: v1
kind: Service
metadata: 
  name: frontend
spec:
  type: LoadBalancer   # 👈 IMPORTANT CHANGE
  selector:
    role: frontend
  ports:
    - protocol: TCP
      port: 80          # 👈 external port
      targetPort: 3000  # 👈 container port (React runs here)